DB_HOST=localhost
DB_USER=root
DB_PASSWORD=MirLloyd-15
DB_NAME=my_website
DB_PORT=3306

# Email (SMTP) — fill in your credentials
# For Gmail: use an App Password (not your normal password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@makandra.com

# The URL where the frontend is served (for email links)
FRONTEND_URL=http://localhost:5500

# Secret key to create the first admin account
ADMIN_SECRET=changeme-super-secret